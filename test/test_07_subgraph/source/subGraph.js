// JavaScript source code

function subGraph(routes_name) {
    this.create_subGraph = load_data;
    this.route = routes_name;


    function load_data() {
        alert(this.route);
        // nacteni dat - REST
        // zobrazeni nactenych dat
    }
}