node = {
    name: name,
    lat: latitude,
    long: longitude,
    type_node: ('S' || 'R'), 
    address: address_node,
    description: description_node,
    hw: hardware,

    id:
    token:
    
};

link = {
    source: source, (melo by byt id z node)
    source_channel: source_port,
    speed: speed_link,
    target: target, (melo by byt id z node)
    target_channel: target_port,
    name: name_link,
    node: "", (mel by byt id z "node")

    id:
};
